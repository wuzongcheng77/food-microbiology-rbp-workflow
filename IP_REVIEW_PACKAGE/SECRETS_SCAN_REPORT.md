# Export privacy scan

Absolute local paths: zero matches across current export text files. Credential patterns: zero. Unapproved email addresses: zero. Supplied corresponding-author email is intentionally public and allowlisted. Binary PNG/TIFF files contain generated plot exports, unchanged from verified source hashes. Pattern-based checks are bounded and do not certify all possible identifiers absent.
